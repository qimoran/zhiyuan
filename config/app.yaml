app:
  # Flask 应用名称。
  name: kaoyan_recommendation
  # 容器内监听地址。
  host: 0.0.0.0
  # Flask 默认端口。若需要宿主机访问，需要在 docker-compose.yml 中映射该端口。
  port: 5000
  # 本地开发可通过环境变量 APP_DEBUG=true 临时开启。
  debug: false
