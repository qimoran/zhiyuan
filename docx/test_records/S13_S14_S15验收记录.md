# S13-S15 验收记录

## S13 HDFS/Hive/Spark 分析链路

交付物：

- `sql/hive/001_create_external_tables.sql`
- `src/data_pipeline/sync_hive.py`
- `src/analysis/spark_score_trend.py`
- `src/analysis/spark_major_heat.py`
- `src/analysis/spark_plan_trend.py`
- `src/analysis/write_analysis_result.py`

建议验收命令：

```powershell
cd "D:\bigdatashixun\zhiyuan"
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python python -m src.data_pipeline.sync_hive --batch-id 20260616_full_v2
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python spark-submit --master "spark://spark-master:7077" "/workspace/src/analysis/spark_score_trend.py" --batch-id 20260616_full_v2
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python spark-submit --master "spark://spark-master:7077" "/workspace/src/analysis/spark_plan_trend.py" --batch-id 20260616_full_v2
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python spark-submit --master "spark://spark-master:7077" "/workspace/src/analysis/spark_major_heat.py" --batch-id 20260616_full_v2
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python python -m src.analysis.write_analysis_result --batch-id 20260616_full_v2
```

说明：`sync_hive` 的 Hive 连接信息从 `.env` 或容器环境变量读取，支持 `HIVE_JDBC_URL`、`HIVE_DB_USER`、`HIVE_DB_PASSWORD` 覆盖；dry-run 和异常日志会隐藏密码。

验收点：

- HDFS `/zhiyuan/ods` 能看到 cleaned CSV。
- Hive `zhiyuan_ods` 库能查询 `ods_majors`、`ods_enrollment_plans`、`ods_score_lines`、`ods_admission_records`。
- `data/analysis/` 生成 Spark 统计 CSV。
- `major_statistics` 写入或更新统计结果。

当前执行结果（2026-06-18）：

- `sync_hive` 成功同步 4 个文件：`majors`、`enrollment_plans`、`score_lines`、`admission_records`。
- HDFS 已存在 `/zhiyuan/ods/majors/majors_20260616_full_v2.csv` 等 ODS 文件。
- Hive `SELECT ... LIMIT 2` 能读取 `ods_enrollment_plans` 和 `ods_score_lines` 中文数据。
- 三个 Spark 任务均已成功运行，输出：
  - `data/analysis/spark_score_trend_20260616_full_v2.csv`
  - `data/analysis/spark_plan_trend_20260616_full_v2.csv`
  - `data/analysis/spark_major_heat_20260616_full_v2.csv`
- `write_analysis_result` 显示计划写回 10136 条、跳过 0 条；MySQL `major_statistics` 当前保留 9939 条唯一统计记录。

## S14 AI 报告模块

交付物：

- `src/services/report_service.py`
- `/api/report/generate`
- `src/templates/report.html`
- `src/static/js/report.js`

建议验收命令：

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:5000/api/report/generate" -Method Post -ContentType "application/json" -Body '{"recommendation_trace_id":"test-trace-id","report_type":"template"}'
```

验收点：

- 未配置大模型时返回模板报告。
- 报告写入 `report_records`。
- 报告包含风险提示和“仅供参考，最终以官方招生政策和当年复试录取结果为准”。

当前执行结果（2026-06-18）：

- `/api/report/generate` 返回 `code=0`。
- 已生成 `report_id=1`。
- MySQL `report_records` 当前有 1 条模板报告记录。
- 当前 Flask 进程已重启为最新代码，容器内进程为 `python -m src.app`。

## S15 集成测试与交付

交付物：

- `tests/unit/test_report_service.py`
- `tests/integration/test_api_contracts.py`
- README 后续模块运行命令
- 本验收记录

建议验收命令：

```powershell
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python python -m compileall /workspace/src
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python pytest "/workspace/tests/unit"
docker compose --profile hadoop --profile hive --profile spark --profile tools exec -T python pytest "/workspace/tests/integration"
```

已知说明：

- 当前没有拟录取名单数据，`admission_records` 和拟录取趋势图可以为空，但接口不能报错。
- 报告模块当前默认使用模板报告，不调用真实大模型。
- 已实际运行一次 S13 命令；正式答辩前建议再保存 Spark Master、HDFS、HiveServer2 页面截图作为展示材料。
